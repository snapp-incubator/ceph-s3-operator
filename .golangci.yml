run:
  skip-dirs:
    - vendor/
output:
  option: value
linters:
  disable-all: true
  enable:
    - goimports
linters-settings:
  goimports:
    local-prefixes: github.com/snapp-incubator/s3-operator
